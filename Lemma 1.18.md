#theorems #graph-theory 

感觉 induction 常用。OMG！怪不得叫 induced subgraph。

The vertices of every [[connected]] graph on $n≥2$ vertices can be ordered $(v_1,...,v_n)$ so that for every $i∈{1,...,n}$ the graph $G[ \{v1,...,vi\}]$([[induced]] subgraph) is connected.