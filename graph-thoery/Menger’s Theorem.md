#theorem
For any graph $G$ and any two vertex sets $A,B ⊆V(G)$, the smallest number of vertices [[separate|separating]] A and B is equal to the largest number of [[independent paths|disjoint A-B-paths]].

Note that Menger’s Theorem implies that if G is [[k-linked]], then G is [[k-connected]].