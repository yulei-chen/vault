#testing 

 = defeat

A flaw in a component or system (e.g., incorrect statement or data definition). It is the **Non-fulfillment of a defined [[requirements|requirements]]**, or a deviation between actual behavior and intended behavior (specification, requirements).