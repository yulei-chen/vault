#saq 

- Defines the set of meta-classes whose instances a [[view]] can display
- Comprises a definition of a [[concrete syntax]] plus a mapping to the [[abstract syntax|abstract meta-model syntax]].
- On [[metamodel]] level